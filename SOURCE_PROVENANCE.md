# Source Provenance

This public-export candidate was created from the isolated directory
`tools/gtfs-preprocessor/` at private source commit:

```text
1fc19893ad122f7f7a7df945bd1ed5d0f4be14e0
```

The implementation commit for transfer and footpath hardening is:

```text
8041a39da5e30e172fd6a6f62bec4f881de7c947
```

The export intentionally starts a new Git history. It excludes the surrounding
application repository, real feeds, generated artifacts, server operations,
historical production audits, and deployment configuration.

